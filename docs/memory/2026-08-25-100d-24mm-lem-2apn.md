# Session 2026-08-25 — 100D 24 mm + plan LEM 2 APN

Branche : `docs/formes-prise-de-vue` (depuis `e647375`). Suite de
[script-seance-lem-600d.md](2026-08-24-script-seance-lem-600d.md).

## Contexte

Forme A : focale et composition (horizon au tiers, MAX au ciel), puis
intérêt de LEM sur le 100D pour **suivre l’aube** (pas un HDR de 50 px).
Test 2 APN demain ; script aube seulement si USB OK.

## Accompli

- `simulate_fov.py` : `framing.composition: horizon_thirds` ; auto-top sur le
  **limbe haut** dans ce mode. YAML
  [`tournefeuille-100d-24mm.yaml`](../../scripts/config/tournefeuille-100d-24mm.yaml).
  Schéma [`fov-100d-24mm-tiers.png`](../figures/fov-100d-24mm-tiers.png).
  Tests FOV étendus (`tests/test_simulate_fov.py`).
- Recette terrain : lock **04:52:44** CEST, Lune tangente bord haut, **20 %
  gauche** ; U1 hors cadre ; MAX à 63 % de hauteur.
- Plan LEM 100D : nom **`100D-W24`** ; bench
  [`bench-2apn.txt`](../../scripts/lem/essais-2026/bench-2apn.txt) (scénario
  simple, trois passes dont allumage tardif). Grille aube 3→5 documentée, **pas
  écrite** en script. `say` cadrage U1+18:50… (DEC-017) dans le README essais.
- Docs : formes A / split / §8 / §13–14 ; Getting Started ; fiche lieu ;
  methode-fov ; tests.md ; AGENTS.md ; architecture-always ; README LEM.

Aucun PR. pytest FOV déjà verts en session (30 cas) ; pas relancés à la clôture.

## Décisions

| ID | Sujet |
|----|-------|
| **DEC-017** | 100D **24 mm** paysage, `horizon_thirds`, lock 04:52:44 bord haut 20 % gauche |
| **DEC-018** | Test 2 APN `100D-W24` **avant** script aube ; grille 3 puis 5 × 2 EV (centrale nuit 4 s · ISO 800 = placeholder) ; AEB = repli ; LEM 100D = suivre le jour, pas un 7 × 2 EV |

DEC-010 : LEM sur le 100D n’est plus rejeté pour *suivre l’aube* (toujours rejeté pour un HDR disque).

## Pièges

| ID | Sujet |
|----|-------|
| KI-007 | 100D USB toujours non testé ; listé dans l’aide APN (EOS 100D / SL1) |
| **KI-023** | Allumage tardif : FAQ batterie = actions suivantes auto, **sans** ⌘R. Lignes ignorées au chargement : souvenir observateur, **option non retrouvée** dans l’aide miroir. Premier TAKEPIC après USB : Incremental **N**. Ne pas recharger pendant une rampe 600D (KI-020). |

KI-021 (`say` courts ASCII) : grammaire cadrage prévue, pas encore dans un script séance 100D.

## Prochaines étapes

1. **Test 2 APN** (`bench-2apn.txt`) : (a) les deux allumés ; (b) 100D éteint au
   ⌘L puis allumé sans ⌘R ; (c) ⌘R seulement si (b) muet. Vérifier la chaîne
   JPEG Fine dans Propriétés disponibles.
2. Si (a) OK → script aube 3→5 + `say` cadrage 04:52:44. Si (a) KO → AEB.
3. Scénario 150 mm f/5 + 600D (KI-019) ; fragment `seance-600d-t150.txt` en
   temps simulé.
4. Optionnel : rampe serrée &lt; 3 s ; 7/7 sur 32 Go ; AEB nuit+aube (calage 4 s
   · ISO 800) ; horizon ~06:44.
